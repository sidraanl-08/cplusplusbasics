#include <iostream> //header file that helps with input/output

//main is the entry point where program initiates 
//while int will specify that main returns an integer to OS 
//standard is the namespace that contains standard c++ functions like cout and cin
//return 0 is the successful termination of program which sends 0 value to OS 

int main() {
    std::cout<<"Hello World";
    return 0;
}
